//! ROS 2 bridge for rosa.
//!
//! Phase 5a — CLI shell-out tools (task 05a): wrap `ros2 ...` subcommands.
//! Phase 5b — Native r2r bridge (task 05b): publish Twist, subscribe Imu, etc.
//!            Gated behind the `ros2-native` Cargo feature.
//!
//! This stub compiles immediately so `cargo build --workspace` passes.

// TODO(task-05a): implement ros2_list_nodes, ros2_list_topics, ros2_topic_echo, ...
// TODO(task-05b): implement ros2_publish_twist, ros2_subscribe_once, ... (#[cfg(feature="ros2-native")])
