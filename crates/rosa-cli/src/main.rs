//! rosa — Rust-first ROS 2 agent CLI.
//!
//! Task 06 builds the full REPL + streaming render.
//! This stub satisfies `cargo build --workspace` and `cargo run -- --help`.

fn main() {
    println!("rosa — Rust rewrite in progress. See ORCHESTRATION.md.");
    println!("Tasks remaining: 03 (llm adapters), 04 (tools), 05 (ros2 bridge), 06 (REPL).");
}
