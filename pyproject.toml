# Python packaging removed in feat/rust-rewrite.
# rosa is now a Rust binary (rosa-cli). See ARCHITECTURE.md for crate layout.
# The [project] / [project.optional-dependencies] blocks that declared
# langchain~=0.3.23 and friends have been deleted — they are replaced by
# hand-rolled Rust adapters in crates/rosa-llm/.

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"
